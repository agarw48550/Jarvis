Place Kokoro voices assets here.
Expected files:
- voices.bin
- voices.json (optional, meta)

You can also store voice folders under this directory, but this server expects voices.bin by default.
These files are not tracked in Git.
